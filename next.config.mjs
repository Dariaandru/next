/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  output: 'export', // ЭТО ОБЯЗАТЕЛЬНО ДЛЯ СТАТИЧЕСКОГО ЭКСПОРТА
};

export default nextConfig;
